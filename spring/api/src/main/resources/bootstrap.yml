spring:
  application:
    name: api
  profiles:
    active: dev
  cloud:
    config:
      username: root
      password: s3cr3t
      discovery:
        enabled: true
        service-id: config-server

