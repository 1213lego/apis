spring:
  profiles:
    active: test,development,production
  cloud:
    config:
      username: root
      password: s3cr3t
      name: ms-postgres
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/